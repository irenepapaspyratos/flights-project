import styled from "styled-components";

const FormElementWithIconStyled = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
`;

export default FormElementWithIconStyled;
